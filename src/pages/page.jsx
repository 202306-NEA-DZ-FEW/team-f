import ArticleCard from "@/components/ArticleCard";
import React from "react";

const page = () => {
    return (
        <div>
            <ArticleCard />
        </div>
    );
};

export default page;
